import { describe, test } from "vitest"
import { Notification } from "./notification"

describe("Notification", () => {
    test("should render", () => {
        //const notification = <Notification variant="default" title="Test Title" description="Test Description" />
        //const render_result = render(notification)
        //TODO
        //expect(render_result.getByText("Test Title")).toBeInTheDocument()
    })
})
